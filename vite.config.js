import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// https://vite.dev/config/
export default defineConfig({
  plugins: [vue()],
  base: '/kbo-numbers-explorer/', // Configured to match the GitHub Pages repository name!
})
